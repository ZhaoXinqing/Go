获取第三方库：
	go get 
	go get -u 

完整且典型的Golang程序包含以下几部分：
	包声明(Package Declaration)
	引入依赖包(Import Packages)
	逻辑函数(Function)
	过程变量(Variables)
	语句和表达式(Statements and Expressions)
	注释(Comments)

代理设置
	set GOPROXY= https://goproxy.cn


defer func() {
	recover()
}()  // 可以拦截panic错误